[package]
name = "embedded_common"
version = "0.1.0"
edition = "2024"

[features]
std = ["serde", "bincode"]
defmt = []


[dependencies]
serde = { workspace = true, optional = true }
bincode = { workspace = true, optional = true }
