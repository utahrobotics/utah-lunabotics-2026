
(
    // Arena boundary walls as thin rectangles (thickness ~0.1m)
    walls: [
        (-0.05, -0.05, 4.62, 0.0),    // South wall
        (-0.05, 8.14, 4.62, 8.19),     // North wall
        (-0.05, -0.05, 0.0, 8.19),     // West wall
        (4.57, -0.05, 4.62, 8.19),     // East wall
    ],

    // Central support column
    // Defined as (center_x, center_y, width, length) in meters
    pillars_rect: [ ],

    pillars: [],

    // Boulder obstacles 
    // Specs: Minimum 3 boulders, 30-40 cm diameter 
    boulders: [
        (2.0, 2.5, 0.17),  
        (3.3, 3.5, 0.19),  
        (4.0, 1.0, 0.16),  
    ],

    // Defined as (center_x, center_y, radius) in meters
    // Specs: Minimum 3 craters, up to 40-50 cm width 
    craters: [
        (3.0, 4.5, 0.23),  
        (3.9, 2.0, 0.21),  
        (1.6, 3.5, 0.24), 
    ],
)
