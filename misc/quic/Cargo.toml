[package]
name = "quic"
version = "0.1.0"
edition = "2024"

[dependencies]
cu-bincode = { workspace = true, features = ["derive", "serde"] }
cobs.workspace = true
quinn = "0.11.9"
rcgen = "0.14.5"
tasker = { path = "../tasker" }
uuid = { version = "1.4", features = ["v4", "serde", "fast-rng"] }
